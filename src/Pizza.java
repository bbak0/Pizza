import java.util.Scanner;

public class Pizza {
    Scanner scan = new Scanner(System.in);
    int R;
    int C;
    int L;
    int H;
    int[][] pizza;

    void readinator() {
        R = scan.nextInt();
        C = scan.nextInt();
        L = scan.nextInt();
        H = scan.nextInt();

        pizza = new int[R][C];

        for (int i = 0; i < R; i++) {
            for (int j = 0; i < C; i++) {
                if(scan.next() == "T" {

                }
            }
        }
    }
}
